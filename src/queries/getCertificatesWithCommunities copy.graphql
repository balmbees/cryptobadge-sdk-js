query getCertificatesWithCommunities(
  $count: Int!
  $after: String
  $issuers: [String!]
) {
  certificatesWithCommunities: certificates(
    first: $count
    after: $after
    issuers: $issuers
  ) {
    totalCount
    edges {
      node {
        id
        animationUri
        backgroundColor
        claimable
        resourceUrl
        issuer
        imageUri
        name
        description
        winner
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
