query getBadgeByIssuerAndPath($issuer: String!, $path: String!) {
  badge(issuer: $issuer, path: $path) {
    id
    name
    path
    imageUrl
    description
    criteria
    backgroundColor
    textColor
    transactionStatus
  }
}
