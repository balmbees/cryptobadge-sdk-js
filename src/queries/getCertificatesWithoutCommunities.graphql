query getCertificatesWithoutCommunities(
  $count: Int!
  $after: String
  $contracts: [String!]
) {
  certificatesWithoutCommunities: certificates(
    first: $count
    after: $after
    contracts: $contracts
  ) {
    totalCount
    edges {
      node {
        id
        animationUri
        backgroundColor
        claimable
        resourceUrl
        issuer
        imageUri
        name
        description
        winner
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
