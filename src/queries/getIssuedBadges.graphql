query getIssuedBadges {
  me {
    badges(first: 100) {
      edges {
        cursor
       	node {
          id
          name
          backgroundColor
          imageUrl
          resourceUrl
          path
          description
          criteria
        }
      }
      pageInfo {
        hasPreviousPage
        hasPreviousPage
      }
    }
  }
}